services:
  scipi:
    build:
      context: .
    container_name: scipi
    stdin_open: true
    tty: true
    volumes:
      - .:/app
    extra_hosts:
      - "host.docker.internal:host-gateway"
      - "${COMPUTERNAME:-nb102237}:host-gateway"
